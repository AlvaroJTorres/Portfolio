import styled from "styled-components";
import tw from "twin.macro";

export const StyledMainContainer = styled.div`
  ${tw`flex flex-col items-center justify-center p-4 gap-6 md:col-span-2 xl:col-span-3`}
`;
