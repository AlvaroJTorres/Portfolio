import styled from "styled-components";
import tw from "twin.macro";

export const StyledTitle = styled.h1`
  ${tw`text-3xl text-center font-serif`}
`;

export const StyledParagraph = styled.p`
  ${tw`text-justify`}
`;
